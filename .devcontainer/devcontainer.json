{
  "name": "Fedora Podman Claude DevContainer",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  // Remove the "features" block completely to avoid the broken build step

  "runArgs": [
    "--userns=keep-id",
    "--security-opt", "label=disable",
    "--device=/dev/dri",
    "--cap-add=PERFMON",
    "--cap-add=SYS_PTRACE"
  ],

  "mounts": [
    {
      "source": "${localEnv:HOME}/.claude",
      "target": "/home/vscode/.claude",
      "type": "bind",
      "options": ["z"]
    },
    {
      "source": "${localEnv:HOME}/.claude.json",
      "target": "/home/vscode/.claude.json",
      "type": "bind",
      "options": ["z"]
    },
    {
      "source": "${localEnv:XDG_RUNTIME_DIR}/${localEnv:WAYLAND_DISPLAY}",
      "target": "/tmp/wayland-0",
      "type": "bind"
    },
    {
      "source": "/tmp/.X11-unix",
      "target": "/tmp/.X11-unix",
      "type": "bind"
    },
    {
      "source": "qt611-volume",
      "target": "/opt/qt611",
      "type": "volume"
    }
  ],

  "containerEnv": {
    "JUNIE_API_KEY": "perm-bGludXhoaXBweUBnbWFpbC5jb20=.MDkzN2RkNmMtMWIyMi00ZmEyLThhNjItYWZlZDQxNTc2YTRi.6yZDTIh5egDk786MloRy0MRTPYShVW",
    "WAYLAND_DISPLAY": "wayland-0",
    "XDG_RUNTIME_DIR": "/tmp",
    "QT6DIR": "/opt/qt611/6.11.1/gcc_64"
  },

  // Install dev tools, Node.js, and Claude Code CLI after container creation
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y curl && curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt-get install -y nodejs qt6-tools-dev qt6-base-dev qt6-multimedia-dev qt6-wayland-dev qt6-base-private-dev ant ant-optional autoconf bison build-essential ccache doxygen flex fontconfig git gperf graphviz junit4 libarchive-zip-perl libavahi-client-dev libcups2-dev libfontconfig1-dev libgstreamer-plugins-base1.0-dev libgstreamer1.0-dev libgtk-3-dev libkf5config-dev libkf5coreaddons-dev libkf5i18n-dev libkf5kio-dev libkf5windowsystem-dev libkrb5-dev libnspr4-dev libnss3-dev libqt5x11extras5-dev libx11-dev libxml2-utils libxrandr-dev libxslt1-dev libxt-dev meson nasm openjdk-17-jdk openjdk-21-jdk python3 python3-dev python3-setuptools qtbase5-dev xsltproc zip libvulkan-dev vulkan-tools vulkan-validationlayers mesa-vulkan-drivers libwayland-dev weston xvfb && sudo npm install -g @anthropic-ai/claude-code && (test -f /opt/qt611/6.11.1/gcc_64/bin/qmake || (sudo mkdir -p /opt/qt611 && sudo chown vscode:vscode /opt/qt611 && pip3 install --user --break-system-packages aqtinstall && /home/vscode/.local/bin/aqt install-qt linux desktop 6.11.1 linux_gcc_64 -O /opt/qt611 -m qtmultimedia))",

  "remoteUser": "vscode"
}
